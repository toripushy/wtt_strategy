[project]
name = "btcore"
version = "0.1.0"
description = "Local strategy backtesting CLI with daily and minute-bar support."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
bt = "btcore.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
